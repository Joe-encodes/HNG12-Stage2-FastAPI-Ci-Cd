server {
    listen 80;
    location / {
        proxy_pass http://127.0.0.1:8000;  # Adjust this if your FastAPI service is named differently
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}